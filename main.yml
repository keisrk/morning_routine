---
# Main playbook targeting localhost only.

- hosts: localhost

  roles:
    - utils
    - user
    - docker
    - adoptopenjdk
    - sbt
    - gradle
    - golang
    - nodejs
    - build
    - dotnet
    - corert
    - dynamodb_local
    - aws_cli
    - mvctrl
    - arm_embedded

- hosts: user

  roles:
    - git
    - rustup
    - wasmpack
    - gradle_path
    - gopath
    - arm_embedded_path
    - python3
    - npm
